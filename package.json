{
  "name": "quote-box",
  "version": "2.0.1",
  "description": "Get a quote and push to GitHub Gist.",
  "main": "dist/index.js",
  "scripts": {
    "format": "prettier --check src/index.ts --write",
    "build": "esbuild src/index.ts --bundle --minify --platform=node --outfile=dist/index.js"
  },
  "author": "Connor Kuo <touch@rnmeow.com>",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.14.9",
    "esbuild": "^0.20.2",
    "prettier": "^3.2.5"
  },
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@octokit/request": "^9.1.1"
  }
}
